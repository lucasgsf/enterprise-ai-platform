# Backend API configuration (read by pydantic-settings, prefix APP_).
# Copy to `.env` and adjust as needed.

APP_APP_NAME="Enterprise AI Platform API"
APP_ENVIRONMENT="development"
APP_DATABASE_URL="postgresql+asyncpg://app:app@localhost:5432/app"
# Note: the app version is NOT configurable here — it is derived from the installed
# package metadata (pyproject.toml), since it is a build artifact, not runtime config.
